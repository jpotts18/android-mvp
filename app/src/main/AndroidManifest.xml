<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.jpotts18.android_mvp" >

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".domain.ui.activities.LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".domain.ui.activities.RepoListActivity"
            android:label="@string/title_repo_list_activity" >
        </activity>
        <activity
            android:name=".domain.ui.activities.RepoListFragmentActivity"
            android:label="@string/title_activity_repo_list_fragment" >
        </activity>
    </application>

</manifest>
